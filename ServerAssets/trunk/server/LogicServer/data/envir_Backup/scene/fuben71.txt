{ --VIP等级BOSS
	--副本id
	fbid = 71,
	--副本的适合等级(推荐等级)，
	recommandLevel = 60,
	--destoryTime = 5,	--副本当没人的时候的销毁时间，如果副本没配置，则默认为全局的，单位秒
	--这个副本需要用到的场景列表
	scenes =
	{
--#include "scene217.txt"
	},

	fbProgressTitles=
	{
		Lang.FBTalk.ts0039,
  
	},
	
	fbProgressTips = Lang.FBTalk.ts0040,
},